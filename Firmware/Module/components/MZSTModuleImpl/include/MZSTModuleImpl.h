/* Module implementation file must define a processCommand function,
    which takes a command (null-terminated string) and does some action.
    Typical invocation is from messageLoop.
*/
void processCommand(char* cmd);